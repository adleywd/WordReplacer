@page "/"
@using WordReplacer.Dto
@using WordReplacer.WebApp.Pages.MainSections
@inject IStringLocalizer<GeneralResource> Localizer

<MudPaper Width="100%" Class="mt-5 mb-5 pb-5 pt-5" Elevation="5">
    <MudContainer>
        
        <UploadFileSection Files="_files" FilesChanged="UpdateUI" />
        <MudDivider DividerType="DividerType.FullWidth" Class="mt-3 mb-3" />
        <ValuesToReplaceFormSection TextsToBeReplaced="_textToBeReplaced" OnAddValues="UpdateUI"/>
    </MudContainer>
</MudPaper>
<MudDivider DividerType="DividerType.FullWidth" Class="mt-3 mb-3" />
<MudPaper Width="100%" Class="mt-5 mb-5 pb-5 pt-5" Elevation="5">
    <MudContainer>
        <MudAlert Severity="Severity.Info" Variant="Variant.Filled" Icon="@Icons.Material.Outlined.Info">
            @Localizer["emptyValueListInfo"]
        </MudAlert>
    </MudContainer>
</MudPaper>

@code {
    
    private readonly Dictionary<string, FileUploadDto> _files = [];
    private readonly List<string> _textToBeReplaced = [];
    private Document _doc = new();
    private DocumentParamsDto _docParamsDto = new();
    
    private void UpdateUI()
    {
        StateHasChanged();
    }
    
    public void HandleWordReplacement()
    {
        
    }
}
